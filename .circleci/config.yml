version: 2
jobs:
  build:
    docker:
      - image: bakerb845/mkl-iscl-build
    steps:
      - checkout
      - run:
          name: Print the Current Time
          command: date
      - run:
          name: List the directory
          command: pwd
      - run:
          name: Build the code - TODO add cmake
          command: ls
          #command: git clone https://github.com/bakerb845/xcloc.git
          #command: ls
